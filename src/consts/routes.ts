export enum ROUTES {
  HOME = "/",
  ABOUT = "about-us",
  EVENTS = "events",
  EVENT = ":eid",
  DONATE = "donate",
}

export enum ANCHORS {
  HOME = "#",
  ABOUT = "#about-us",
  EVENTS = "#events",
  DONATE = "#donate",
}

export enum IDS {
  HOME = "main",
  ABOUT = "about-us",
  EVENTS = "events",
  DONATE = "donate",
}
